rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Сценарии только у владельца (anonymous uid)
    match /users/{userId}/scenarios/{scenarioId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
